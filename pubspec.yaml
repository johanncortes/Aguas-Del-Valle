name: aguas_monte_patria
description: "Water Meter Reader MVP for Aguas del Valle - Monte Patria"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State Management
  flutter_riverpod: ^2.6.1

  # Mapping
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Excel Export
  excel: ^4.0.6

  # File System & Sharing
  path_provider: ^2.1.5
  share_plus: ^10.1.4

  # UI & Fonts
  intl: ^0.20.2
  dio: ^5.11.0
  dio_cache_interceptor: ^4.0.7
  uuid: ^4.6.0
  geolocator: ^14.0.2
  file_picker: ^11.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  geolocator_platform_interface: ^4.2.0
  path_provider_platform_interface: ^2.1.2

flutter:
  uses-material-design: true

